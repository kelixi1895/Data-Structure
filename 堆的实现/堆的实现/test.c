#define _CRT_SECURE_NO_WARNINGS 1
#include "heap.h"

int main() {
	//int arr[] = { 93, 72, 48, 53, 45, 30, 18, 36, 15, 35};
	///*Heap hp;
	//HeapCreate(&hp, arr, sizeof(arr) / sizeof(arr[0]));
	//HeapPrint(&hp);
	//HeapPush(&hp, 22);
	//HeapPrint(&hp);
	//printf("HeapPop:\n");
	//HeapPop(&hp);
	//HeapPrint(&hp);
	//HeapPop(&hp);
	//HeapPrint(&hp); HeapPop(&hp);
	//HeapPrint(&hp); HeapPop(&hp);
	//HeapPrint(&hp);*/
	//HeapSort(arr, 10);
	//for (int i = 0; i < sizeof(arr) / sizeof(arr[0]); i++) {
	//	printf("%d ", arr[i]);
	//}
	//printf("\n");
	
	TestTopk();
	system("pause");
	return 0;
}